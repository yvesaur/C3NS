<atom> := INT|FLOAT|IDENTIFIER
       := <LPAREN> <expr> <RPAREN>

<power> := <atom> (<POW> <factor>)*

<factor> := (PLUS|MINUS) factor
         := <power>

<term> := <factor> ((MUL|DIV) <factor>)*

<expr> := <term> ((PLUS|MINUS) <term>)*
       := <keyword>:<var> <ident> <eq> <expr>

